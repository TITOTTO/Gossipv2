<div class="container-fluid my-5">
	<h3>Incroyable, on retrouve tous les potins ici !</h3>
</div>
<% @gossip.each_with_index do |gossip, index| %>
<div class="card align-self-center my-2" style="width: 100rem;">
  <div class="card-body">
    <h5 class="card-title"><%= link_to "#{@gossip[index].title}", "/gossip/#{@gossip[index].id}" %>   </h5>
    <p class="card-text"><%= @gossip[index].content %></p>


				
			
			<ul> <%= @gossip[index].created_at %> </ul>
<%= link_to "Supprimer", gossip_path(gossip), method: :delete, class: "btn btn-danger", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>

										</div>
									</div>

 <% end %>
