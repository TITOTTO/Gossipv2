<div class="container-fluid my-5">
	<h3>Voici la page de <%= @user.first_name %> <%= @user.last_name %></h3><br>
  Voici sa description <%= @user.description%><br>
  Voici son adresse mail <%= @user.mail%><br>
  Son age <%= @user.age%> ans<br>
  <%= link_to "#{@user.city.name}", "/city/#{@user.city.id}" %>
</div>


<% @user.gossips.each do |gossip| %>
<div class="card align-self-center mt-2" style="width: 100rem;">
  <div class="card-body">
    <h5 class="card-title"><%= link_to "#{gossip.title}", "/gossip/#{gossip.id}" %> </h5>
    <p class="card-text"><%= gossip.content %></p>	
				</div>
									</div>

 <% end %>
 <% if logged_in? && params[:id].to_i == current_user.id.to_i %>
          <%= link_to "Créer un gossip", "/users/#{@user.id}/gossip/new" %>
<% end %>
